{
  "name": "neon-arrow-nexus",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AIGC coursework neon arrow puzzle game",
  "scripts": {
    "start": "node scripts/server.mjs",
    "test": "node tests/engine.test.mjs",
    "check": "node --check src/engine.js && node --check src/game.js && node --check scripts/server.mjs && node tests/engine.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
